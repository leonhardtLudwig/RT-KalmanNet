Tasks:

Improve the existing robust KalmanNet implementation (provided in CODE/RT_KFNET) by completing the following tasks:

1. Apply the backpropagation-through-time (BPTT) algorithm to train the RT-KalmanNet described in Section 3.D.

2. Replace the core neural network architecture of the model with a Gated Recurrent Unit (GRU).

3. Compare the parameter c learned by the proposed network with the true value of c under the following scenarios:
   1). c is constant;
   2). c varies slowly;
according to a constant-velocity (CV) model, using least-favorable data generated by the implementation provided in CODE/LF_DATA.

4. Compare the state estimation performance of the EKF, REKF, KalmanNet, the existing robust KalmanNet, and the proposed method for the nonlinear case as in Section 4.C.

(The code for KalmanNet is in https://github.com/KalmanNet/KalmanNet_TSP)